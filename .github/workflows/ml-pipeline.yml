name: ML Pipeline - Pretrained Model

on:
  push:
    branches: [ master ]
  pull_request:
  workflow_dispatch:

jobs:
  build_runtime:
    name: Build Runtime Docker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Runtime Docker Image
        run: |
          docker build --target runtime -t ghcr.io/${{ github.repository_owner }}/speech-model:latest .

      - name: Push Docker Image to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - run: |
          docker push ghcr.io/${{ github.repository_owner }}/speech-model:latest

  validate_model:
    name: Validate Model & Measure Latency
    runs-on: ubuntu-latest
    needs: build_runtime
    steps:
      - uses: actions/checkout@v3

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install torch torchaudio numpy

      - name: Run Validation with Pretrained Model
        run: python validate_model.py




